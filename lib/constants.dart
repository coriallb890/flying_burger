import 'package:flutter/material.dart';

const bluePrimaryColor = Color(0xFF0064B0);
const redPrimaryColor = Color(0xFFb4171E);

const appBarTitle = TextStyle(
    color: redPrimaryColor,
    fontFamily: 'Bowlby',
    fontSize: 45,
    letterSpacing: 2
);

const redTitle = TextStyle(
  color: redPrimaryColor,
  fontFamily: 'Bowlby',
  fontSize: 30,
  letterSpacing: 2
);

const menuTitle = TextStyle(
    color: redPrimaryColor,
    fontFamily: 'Bowlby',
    fontSize: 24,
    letterSpacing: 2
);